# Python 3.10–3.11 recommended
# Pinned set to avoid resolver conflicts
fastapi
uvicorn
pydantic
python-dotenv
streamlit
requests

# LangChain + ecosystem (pinned, mutually compatible)
langchain>=0.3.24,<0.4
langchain-community>=0.3.5,<0.4
langgraph>=0.2.76,<0.3
langchain-ollama>=0.2.0,<0.4
langchain-huggingface>=0.1.0,<0.4


# RAG stack
# NOTE: On Apple Silicon, if this fails, try: pip install faiss-cpu==1.7.4
faiss-cpu
sentence-transformers==2.6.1
pypdf==4.3.1

# Whisper STT (needs ffmpeg installed on the system)
openai-whisper

# Torch for sentence-transformers and whisper
# For Apple Silicon with CPU-only use this; for Metal (MPS) you can also use 2.3.x
torch==2.2.2
torchaudio==2.2.2
